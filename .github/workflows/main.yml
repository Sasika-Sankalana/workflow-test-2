name: Sync master to release

on:
  push:
    branches:
      - master

jobs:
  sync-branches:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Fetch all branches
        run: git fetch origin

      - name: Merge master into release
        id: merge
        run: |
          git checkout poc-release-sync
          git merge origin/master --allow-unrelated-histories || echo "::set-output name=conflict::true"

      - name: Push changes to release
        if: steps.merge.outputs.conflict != 'true'
        run: git push origin poc-release-sync

      - name: Send Slack notification on merge conflict
        if: steps.merge.outputs.conflict == 'true'
        run: |
          echo "Sending Slack notification."
          curl -X POST -H 'Content-type: application/json' \
          --data '{
            "text": "Merge conflict detected between `master` and `poc-release-sync` branches. Please resolve it manually.",
            "attachments": [
              {
                "color": "#ff0000",
                "fields": [
                  {
                    "title": "Branch",
                    "value": "master -> poc-release-sync",
                    "short": false
                  }
                ]
              }
            ]
          }' ${{ secrets.SLACK_WEBHOOK_URL }}